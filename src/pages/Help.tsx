import { BookOpen, BrainCircuit, MessageCircle, Upload } from 'lucide-react';
import { useNavigate } from 'react-router-dom';

const guide = [{ icon: Upload, title: 'Add a source', copy: 'From the dashboard, upload a PDF. Curator creates a course, outline, and starting flashcards from that document.' }, { icon: MessageCircle, title: 'Study in conversation', copy: 'Open a session and ask questions in your own words. The tutor uses the active course as its context.' }, { icon: BookOpen, title: 'Review the deck', copy: 'Use ratings to change card mastery. Analytics and weak-area signals update from those real reviews.' }];
export default function Help() { const navigate = useNavigate(); return <div className="page page-enter space-y-5"><section className="panel-dark p-7 sm:p-10"><p className="eyebrow text-white/70 before:bg-sun">A short field guide</p><h1 className="mt-5 max-w-2xl font-display text-4xl font-bold leading-[.95] tracking-[-.07em] text-white sm:text-6xl">The workspace follows your material.</h1><p className="mt-6 max-w-xl text-sm leading-7 text-white/70">Start with a source, explore it in a session, and keep the ideas you want to revisit in your deck.</p></section><section className="grid gap-4 md:grid-cols-3">{guide.map((item, index) => <article key={item.title} className="panel p-6"><div className={`grid h-12 w-12 place-items-center rounded-2xl ${index === 0 ? 'bg-sun' : index === 1 ? 'bg-mint' : 'bg-sky/30'} text-ink`}><item.icon size={21} /></div><h2 className="mt-8 font-display text-2xl font-bold tracking-[-.05em] text-ink">{item.title}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{item.copy}</p></article>)}</section><section className="panel p-6 sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-center"><div><p className="eyebrow">Start here</p><h2 className="section-title mt-3">Ready to build a course?</h2></div><button className="button-primary" onClick={() => navigate('/')}><BrainCircuit size={17} />Open dashboard</button></div></section></div>; }
